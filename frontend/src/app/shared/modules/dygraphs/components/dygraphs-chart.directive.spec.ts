import { DygraphsChartDirective } from './dygraphs-chart.directive';

describe('DygraphsChartDirective', () => {
  it('should create an instance', () => {
    const directive = new DygraphsChartDirective();
    expect(directive).toBeTruthy();
  });
});
