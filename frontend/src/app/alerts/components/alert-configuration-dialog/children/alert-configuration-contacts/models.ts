export enum Mode {
    all = 'ALL',
    edit = 'EDIT',
    editRecipient = 'EDITRECIPIENT',
    createRecipient = 'CREATERECIPIENT',
}

export enum RecipientType {
    OpsGenie,
    Slack,
    HTTP,
    OC,
    Email
}
