import { NavigatorPanelItemDirective } from './navigator-panel-item.directive';

describe('NavigatorPanelItemDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigatorPanelItemDirective();
    expect(directive).toBeTruthy();
  });
});
